# Midnight Royal — Markdown showcase

Open this file to review Markdown token colours.

## Heading (purple, bold)

Body text uses the default editor foreground (`foam`).

**Bold / italic emphasis** should pick up the yellow markup style.

Here is a [link](https://example.com) (blue).

Inline `code` and a fenced block:

```ts
// Code inside Markdown still uses editor styling for the language
const note = "syntax-showcase.tsx has the full token tour";
```

> Block quote — useful to glance at quote chrome if your Markdown preview/theme styles it.

- List item one
- List item two

| Column | Value |
|--------|-------|
| Purple | headings / keywords |
| Blue   | links / functions |
| Green  | strings |
| Yellow | types / emphasis |
